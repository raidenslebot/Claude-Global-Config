{
  "name": "claude-global-config",
  "version": "1.48.0",
  "private": true,
  "description": "Claude Code global config — installer, doctor, secret scanner and their tests.",
  "//type": "Deliberately NOT \"module\": config/hooks/*.js are CommonJS and are loaded by basename. A root type:module would reinterpret them as ESM and break every hook run from inside the repo. Tests are .mjs, so they are ESM regardless.",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "cgc": "tools/cgc.mjs"
  },
  "scripts": {
    "test": "node tools/run-tests.mjs",
    "install-config": "node tools/install.mjs",
    "doctor": "node tools/doctor.mjs",
    "sync": "node tools/sync.mjs",
    "scan": "node tools/scan-secrets.mjs"
  },
  "license": "MIT",
  "dependencies": {
    "fontkit": "^2.0.4"
  }
}
